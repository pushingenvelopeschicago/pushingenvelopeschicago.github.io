import * as React from 'react';
import { InteractiveLink } from '../ui/InteractiveLink';
import { P } from '../ui/Paragraph';

export const DonateComponent: React.VFC = () => (
  <div>
    <P>
      Donate
    </P>
  </div>
);
